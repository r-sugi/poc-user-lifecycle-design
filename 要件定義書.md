# 要件定義書

## 1. 目的・背景

### 1.1 主目的

本プロジェクトの主目的は、**ユーザーのステータス・ライフサイクル検証**である。

- **管理画面**: ユーザー一覧 → 詳細で、ステータスおよび付随情報（プロフィール、認証手段、セッション、ステータス変更履歴とその理由）をすべて閲覧でき、その場で管理者操作（BAN・BAN解除など）ができること
- **ユーザー画面**: サインアップ → メール検証 → ログイン → 退会 → 退会取消 → 再ログインといった、ステータスが変化する操作を画面上で順番に実行できること

API（JSON）は自動化・裏側の疎通確認用として維持するが、**UI が第一の検証手段**である。

### 1.2 背景 — ライフサイクル分離パターンの適用理由

「消えるタイミングが違うものは、同じテーブルに置かない」という **ライフサイクル分離パターン** を適用する。

ユーザー周りは寿命パターン **D（独立）** — 状態が変わり、終端が削除 — に該当する。判定の流れは次のとおり。

> 状態が変わるか → 変わるなら寿命は親と別か → 別なら終端は削除か  
> 3つとも Yes が D。

D ドメインでは、問いごとに必要な部品が決まる。

| 問い | 本プロジェクトでの答え | 要する部品 |
|---|---|---|
| 削除要求が来るか | 来る（退会後の PII 物理削除） | `user_profiles` への PII 分離 |
| 状態が変わるか | 変わる（active / withdrawn / banned） | 状態の1箇所集約 + 更新を1本の経路に寄せる |
| なぜそうなったか聞かれるか | 聞かれる（BAN・退会の異議申し立て等） | `user_status_events` + 種別ごとの詳細テーブル |

これらを最初から分離しないと、後述のアンチパターン（論理削除フラグの多重化、理由の分からない BAN、未検証ユーザーの混入、`users.email` UNIQUE による再登録不能、BAN してもログインできる等）に陥りやすい。本 PoC は、その分離設計が実際にステータス遷移を正しく支えられるかを、画面操作で検証する。

---

## 2. スコープ

### 2.1 スコープ内

#### API（原設計 §12 ベース、ヒアリング確定の差分あり）

| 分類 | 機能 |
|---|---|
| サインアップ | 申込 / URL 検証 / 認証メール再送 |
| ログイン | パスワードログイン、ログアウト |
| Google 認証 | モック開始 + コールバック（実 Google API は使わない） |
| パスワード | リセット要求 / リセット実行、ログイン後のパスワード変更 |
| メール変更 | 要求 / 検証（旧アドレスへも通知ログ） |
| 自分情報 | `GET /me`（プロフィール更新 API は実装するが画面化はしない） |
| 退会 | 退会 / 退会取消 |
| 管理者 | ログイン・ログアウト、ユーザー検索・詳細・履歴、BAN / BAN解除 |
| バッチ | 退会猶予経過分の PII 削除、消費済み・期限切れトークン削除 |

**除外する原設計エンドポイント**: `POST /auth/refresh`（セッション 7 日固定・延長なしのため廃止）

#### UI（共通・検証トップ）

- **検証トップ**（`GET /`）: ローカル POC の入口。シード済み確定ユーザーの要約一覧、**未認証（`signup_verifications`）の一覧**、ユーザー向け／管理者向け画面へのリンク。認証不要。両一覧とも列名は **「初期状態」「現在の状態」**（曖昧な「状態」は使わない）。未認証セクション見出しは「メール確認待ち（users未作成）」

#### UI（ユーザー向け）

- サインアップ
- ログイン
- マイページ（自分のステータス・付随情報表示、退会 / 退会取消 / ログアウト、パスワードリセット申請・メール変更申請への導線）
- Google 認証モック同意画面
- パスワードリセット申請 / 実行（メールリンク遷移先）
- メールアドレス変更申請 / 検証結果（メールリンク遷移先）
- サインアップ検証結果（メールリンク遷移先）

#### UI（管理者向け）

- 管理者ログイン
- ユーザー一覧（email 検索）
- ユーザー詳細（ステータス・付随情報・セッション・履歴の全表示、BAN / BAN解除操作）

### 2.2 スコープ外

- ログイン試行回数によるロックアウト（入れる場合は `users.status` とは別軸で持つこと）
- 二要素認証
- Google 以外の OAuth provider
- Stripe 等の課金 Webhook（該当テーブル未設計のため）
- プロフィール編集（表示名など）の **UI 画面化**（API `PATCH /me` は実装可だが、ライフサイクル検証の対象外）
- セッション一覧・個別ログアウトの専用画面（マイページ／管理者詳細での一覧表示はスコープ内）
- 自動テスト（README / tasklist の curl・画面操作による手動確認で代替）
- 本番デプロイ・CORS 付き別オリジン SPA（同一オリジンの SSR + API のみ）

---

## 3. 検証成功の定義

以下が **画面操作のみ** で完結することをもって、本 PoC の検証成功とする。

### 3.1 ユーザー側ライフサイクル

1. サインアップ申込
2. ログに出力されたアクティベーション URL でメール検証
3. ログイン
4. マイページでステータス・付随情報を確認
5. 退会
6. 退会取消
7. 再ログイン

付随シナリオとして、パスワードリセットおよびメールアドレス変更も画面＋ログ URL で完結できること。

### 3.2 管理者側ライフサイクル

1. 管理者ログイン
2. ユーザー一覧から対象を検索し詳細を開く
3. BAN → 対象ユーザーがログイン不可であることをユーザー画面で確認
4. BAN 解除 → 再ログイン可能であることを確認

詳細画面で status / profile / identities / sessions / status_events（理由付き）が閲覧できること。

### 3.3 検証トップでの状態可視性

1. シード投入直後、確定ユーザー一覧・未認証一覧の各行で **初期状態** と **現在の状態** が設計書 §16.2.3 / §21 のカタログどおりに見えること
2. ユーザー操作後（例: U01 退会）、**初期状態は不変**のまま、**現在の状態だけ**が派生ラベルとして更新表示されること（例: 初期 `active / passwordのみ` → 現在 `withdrawn / 退会後30日未満（取消可・PII残）`）
3. 未認証 S1〜S5 の差がトップ一覧の「現在の状態」だけで判別できること

---

## 4. 非機能要件

| ID | 要件 | 詳細 |
|---|---|---|
| NFR-1 | 複数 DB 更新の原子性 | D1 の対話的 `transaction()` は使わず、**`db.batch([...])`** による複数ステートメントの原子的一括実行で保護する。楽観ロック付き更新は「単発 UPDATE → 影響行数確認 → 成功時のみ batch」の 2 段階方式（詳細は設計書） |
| NFR-2 | メール送信 | 実送信しない。件名・宛先・本文相当とトークンから組み立てた URL を **console.log（構造化 JSON）** で出力し、手動で後続操作できること |
| NFR-3 | 動作確認環境 | **ローカルのみ**（`vite dev` + ローカル D1 + ローカル KV / Queues） |
| NFR-4 | セッション失効ラグ | BAN / 退会 / パスワード変更・リセット後の KV セッション削除は Queue 経由。**最大約 60 秒のラグを許容**する |
| NFR-5 | 同一オリジン | SSR 画面と API は同一オリジン。CORS 設定は不要。Cookie は `SameSite=Lax` |
| NFR-6 | スキーマ変更 | `drizzle-kit push` は使わず、`generate` → `wrangler d1 migrations apply --local` のマイグレーション運用に一本化 |
| NFR-7 | 自動テスト | 作成しない。手動確認手順を README / tasklist に記載 |

---

## 5. 技術要件

| 区分 | 技術 |
|---|---|
| ランタイム | Cloudflare Workers |
| フレームワーク | Hono |
| 言語 | TypeScript |
| DB | Cloudflare D1 |
| ORM | drizzle-orm（指定バージョン: 0.44.6） / drizzle-kit（0.31.4） / better-sqlite3（11.8.1、drizzle-kit 内部依存） |
| バリデーション | valibot（最新安定版）+ `@hono/valibot-validator`。HTTP API（JSON）および SSR 画面のフォーム入力を Route（presentation）層で検証 |
| Lint / Format | @biomejs/biome（2.2.5） |
| UI | Tailwind CSS v4 + `hono/jsx` SSR（`hono/jsx-renderer` + `vite-ssr-components`） |
| ビルド / 開発 | create-hono の `cloudflare-workers+vite` テンプレート、`@cloudflare/vite-plugin`、起動は `vite dev` 基本 |
| セッション | Workers KV（`SESSIONS_KV`） |
| セッション一括失効 | Cloudflare Queues（`session-revocations` + DLQ） |
| プロジェクト配置 | テンプレート生成物を **リポジトリ直下** に配置（サブディレクトリを掘らない）。生成直後に依存を最新化し、その後 drizzle / biome / valibot 等を追加 |

アーキテクチャは DDD like なクラスベース層構成（`route → usecase → service → repository / gateway`）。詳細は設計書を参照。

---

## 6. UI 画面一覧

### 6.0 検証トップ（認証不要）

| 画面 | メソッド / パス | 役割 |
|---|---|---|
| 検証トップ | `GET /` | POC 検証の入口。シード確定ユーザー要約、**未認証（`signup_verifications`）一覧**。両表に **「初期状態」「現在の状態」** 列。未認証は見出し「メール確認待ち（users未作成）」。各画面へのリンク |

詳細表示要件は設計書 §16.2。

### 6.1 ユーザー向け

| 画面 | メソッド / パス | 役割 |
|---|---|---|
| サインアップ | `GET/POST /signup` | 申込フォーム |
| サインアップ検証結果 | `GET /auth/signup/verify` | メールリンク遷移先・結果表示 |
| ログイン | `GET/POST /login` | パスワードログイン |
| マイページ | `GET /mypage` | 自分の status / profile / identities / sessions 表示、各種導線 |
| 退会 | `POST /mypage/withdraw` | 退会実行（PRG） |
| 退会取消 | `POST /mypage/withdraw/cancel` | 退会取消（PRG） |
| ログアウト | `POST /logout` | 当該セッション即時削除 |
| Google 開始 | `GET /auth/google` | クエリでダミープロフィールを受け取る |
| Google 同意（モック） | （モック途中画面） | 疑似同意 UI |
| Google コールバック | `GET /auth/google/callback` | 処理後 mypage へリダイレクト |
| PW リセット申請 | `GET/POST /password/reset-request` | 申請フォーム |
| PW リセット実行 | `GET/POST /password/reset` | メールリンク遷移先 |
| メール変更申請 | `GET/POST /mypage/email-change` | 変更申請フォーム |
| メール変更検証 | `GET /email-change/verify` | メールリンク遷移先・結果表示 |

### 6.2 管理者向け

| 画面 | メソッド / パス | 役割 |
|---|---|---|
| 管理者ログイン | `GET/POST /admin/login` | 管理者認証 |
| 管理者ログアウト | `POST /admin/logout` | 管理者セッション削除 |
| ユーザー一覧 | `GET /admin/users` | email 検索付き一覧（確定 `users` のみ） |
| ユーザー詳細 | `GET /admin/users/:id` | 全付随情報・履歴表示 |
| BAN | `POST /admin/users/:id/ban` | BAN 実行 |
| BAN 解除 | `POST /admin/users/:id/unban` | BAN 解除 |

---

## 7. 用語集

| 用語 | 意味 |
|---|---|
| **ライフサイクル分離パターン** | 消えるタイミングが違うものを同じテーブルに置かない設計原則 |
| **寿命パターン A〜E** | 静的 / 従属 / 不滅 / 独立 / 凍結。本システムの users 周りは主に D（独立） |
| **PII** | Personally Identifiable Information。本プロジェクトでは主に `user_profiles`（email, display_name）およびセッションの ipAddress 等 |
| **トークン** | サインアップ検証・パスワードリセット・メール変更・セッション等に使う秘密値。DB / KV には **ハッシュのみ**保存し、生値は Cookie またはメールログに限定 |
| **token_hash** | トークン生値の SHA-256 ハッシュ |
| **寿命パターン（テーブル区分）** | 本体（users）/ 付随情報（profiles・identities）/ 接続（KV sessions）/ 変更履歴（events）/ 使い捨てトークン の5区分 |
| **楽観ロック（last_seq）** | `users.last_seq` を条件にした UPDATE で、ステータス変更の競合を検知する仕組み |
| **batch（D1）** | 複数ステートメントを原子的に実行する D1 / Drizzle の API。本プロジェクトでは対話的 transaction の代替 |
| **セッション失効 Queue** | D1 コミット後に `{ userId, reason }` を投入し、KV 上の該当ユーザーセッションを一括削除する非同期処理。最大約 60 秒のラグを許容 |
| **PRG** | Post-Redirect-Get。画面 POST 後にリダイレクトし、flash Cookie でメッセージを伝える |
| **退会後PII削除待機（旧称: 退会猶予）** | 退会後すぐには PII を消さず保持する期間（既定 **30 日**）。期間内は退会取消可・PII 残存。経過後バッチで `user_profiles` / `user_identities` を物理削除。表示ラベルは `退会後30日未満…` / `退会後30日経過…` |
| **理由コード（ReasonCode）** | BAN / 退会などの定型理由。詳細は `user_bans` / `user_withdrawals` に保存 |
| **actor_type** | ステータス変更の主体。`user` / `admin` / `system` |

---

## 8. 初期データ（シード）

ローカル検証をすぐ始められるよう、マイグレーション適用後に `scripts/seed.ts` で次を投入する（詳細一覧・email・関連状態は **設計書 §21**）。

| 種別 | 件数 | 内容 |
|---|---|---|
| 管理者 | 5人 | 異なる email。共通パスワード既定 `AdminPass123!`（実装時 hash） |
| 確定ユーザー | 16人 | `users` に存在する検証済みユーザー。`active` / `withdrawn` / `banned` と identities・履歴・未消費トークン等のバリエーション。U16（`consumed.verify@example.com`）は S3 消費済み verification に対応。あわせて **`seed_user_labels` に初期状態ラベルを固定 INSERT**（操作で不変） |
| 未認証（メール確認待ち） | 別枠 **5行** | `signup_verifications`（原則 **`users` に入れない**。例外: 消費済み S3 は verify 成功後の正常系として対応確定ユーザー U16 あり）。有効・未消費 / 期限切れ・未消費 / 消費済み・users作成済 / 再送ペア（旧無効化＋現行有効）。**`seed_signup_labels` に初期状態ラベル＋POC 用 `raw_token` を固定 INSERT**。検証トップ `GET /` で一覧し、有効行は認証リンク、無効行は理由表示 |

- ステータス enum は要件どおり 3 値。退会後 PII 削除待機中は `withdrawn` のまま、ラベルで「退会後30日未満（取消可・PII残）」等と表現する
- 検証トップの確定ユーザー「ログイン」はローカル向け `POST /dev/login-as`（パスワード不要・Googleのみも可）。banned / withdrawn は不可。`APP_ENV=production` では無効
- 検証トップの「現在の状態」は `CurrentLifecycleStateResolver` が合成する派生ラベル（設計書 §16.2）
- KV セッションはシードしない（ログインで作成）
- 画面・管理画面でのライフサイクル確認は、本シードを起点にしてもよい（新規サインアップからの一連シナリオもスコープ内のまま）
- 未認証行の email・日時・consumed 有無・初期状態ラベルの確定表は **設計書 §21.5**

---

## 9. 関連ドキュメント

- `設計書.md` — アーキテクチャ、スキーマ、API、セッション、バッチ、**シードデータ（§21）** 等の実装仕様
- `tasklist.md` — 実装フェーズ別チェックリスト
